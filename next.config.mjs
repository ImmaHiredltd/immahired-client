/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['immahire.s3.ca-central-1.amazonaws.com', 'flagdownload.com', 'immahired.cn'],
      },
};

export default nextConfig;
